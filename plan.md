# План следующего этапа разработки

## 1. Рефакторинг WebSocket-логики

- [x] Проанализировать текущую реализацию в `src/pages/dashboard/DashboardPage.vue`.
- [x] Вынести настройки WebSocket в общий конфиг, например `src/shared/config/websocket.ts`:
  - [x] URL сервера;
  - [x] `CLIENT_ID`;
  - [x] задержку переподключения;
  - [x] типы сообщений.
- [x] Вынести работу с WebSocket в переиспользуемый composable, например `src/shared/lib/websocket/useAppWebSocket.ts`.
- [x] Обеспечить возможность использовать WebSocket-соединение в нескольких местах приложения без дублирования логики.
- [x] Синхронизировать состояние WebSocket с Pinia store `watering`.
- [x] Убрать из `DashboardPage.vue` прямое управление переподключением, регистрацией клиента и обработкой статусов устройства.
- [x] Проверить, что команда `LED_ON` продолжает отправляться с dashboard.

## 2. Система шаблонов приложения

- [x] Сформировать слой шаблонов/layouts, например `src/app/layouts` или `src/widgets/layouts`.
- [x] Разделить текущий `AppLayout` на структурные части:
  - [x] `AppHeader`;
  - [x] `AppFooter`;
  - [x] `DefaultLayout`.
- [x] Вынести навигацию из layout в header.
- [x] Добавить footer с базовой информацией о клиенте, версии или статусе приложения.
- [x] Подключить `DefaultLayout` на страницах приложения.
- [x] Оставить возможность добавлять другие шаблоны в будущем, например auth-layout или empty-layout.

## 3. Перенос статусов подключения в header

- [x] Вынести отображение состояния подключения к серверу из `DashboardPage.vue` в `AppHeader`.
- [x] Вынести отображение состояния подключения к устройству ESP32 из `DashboardPage.vue` в `AppHeader`.
- [x] Читать статусы в header из Pinia store `watering`.
- [x] Добавить компактные индикаторы:
  - [x] статус WebSocket-сервера;
  - [x] статус ESP32-устройства.
- [x] Удалить блок статусов из `DashboardPage.vue`.
- [x] Оставить на dashboard только содержимое панели управления и действия пользователя.

## 4. Проверка результата

- [x] Проверить работу навигации после изменения шаблонов.
- [x] Проверить, что статусы в header обновляются при сообщениях WebSocket.
- [x] Проверить повторное использование WebSocket-логики минимум в двух местах или подготовить API composable для такого использования.
- [x] Запустить проверки:

```bash
npm run lint
npm run build
```
